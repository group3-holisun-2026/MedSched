GlobalExceptionHandler (@RestControllerAdvice) — building this next. Needs to catch at minimum: EntityNotFoundException/similar → 404, IllegalStateException/conflict-style exceptions → 409, and java.time.DateTimeException (thrown by DoctorMapper's DayOfWeek.of(value) when WorkingHoursDto.dayOfWeek is outside 1–7) → 400. Note: ResponseStatusException (used throughout RoomService/EquipmentService/ClinicalServiceService) already self-handles via Spring, so the handler is for unchecked exceptions that currently bubble to a raw 500.
WorkingHoursDto.dayOfWeek has no bounds validation (@Min/@Max or similar) — currently only a runtime DateTimeException catches out-of-range values, and only once the handler above exists.
No overlap validation for same-day overlapping work_schedules ranges — DB only blocks exact duplicates via uc_doctor_day_start. Undecided: validate in AvailabilityValidatorService vs. a DB EXCLUDE constraint.
ClinicalServiceService.delete() doesn't check existence before deleting — same 404-vs-500 issue the handler should cover.
EquipmentResponse only exposes id/name/active — doesn't return equipmentTypeId/roomId, even though those are now required/settable on create. Worth adding before F-102 is "done."
Design smell: EquipmentResponse is used for both Equipment (has active) and EquipmentType (doesn't) — recommended fix is splitting out a proper EquipmentTypeResponse DTO instead of faking active = true.
active field on ClinicalServiceRequest — undecided whether new services can be created with active=false, or must always default active and be deactivated separately (had a //unsure about this comment in the original code).
Naming/injection-style inconsistency: ClinicalServiceService/ClinicalServiceServiceInterface naming is inverted vs. DoctorService/DoctorServiceImpl; some services still use field injection instead of constructor injection via @RequiredArgsConstructor.
pom.xml — duplicate mapstruct dependency versions (1.6.3 vs 1.5.5.Final) causing a Maven build warning; also confirm mapstruct-processor and lombok-mapstruct-binding are present (this was flagged early on and not yet re-verified after the dependency-version warning appeared).