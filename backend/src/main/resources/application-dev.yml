spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/medsched_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:EPIC}
    driver-class-name: org.postgresql.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: medsched2026@gmail.com
    password: ${ACCOUNT_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update # Creează/actualizează tabelele automat în dev
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect